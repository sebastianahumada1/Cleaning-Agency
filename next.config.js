/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 16 configuration
  // Turbopack is the default dev server (faster than Webpack)
  // To use Webpack instead, run: npm run dev -- --webpack
}

module.exports = nextConfig

